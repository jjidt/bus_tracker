<nav role="navigation" class="navbar navbar-default">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to "Bus Tracker", root_path, :class => "navbar-brand" %>
    </div>
    <!-- Collection of nav links, forms, and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">

            <li class="dropdown">
                <a data-toggle="dropdown" >Lines<b class="caret"></b></a>
                <ul role="menu" class="dropdown-menu">
                    <li><%= link_to "Create New Line", new_line_path %></li>
                    <li class="divider"></li>
                    <% Line.all.order(:name).each do |line| %>
                        <li><a href=<%= line_path(line) %>><%= line.name %> </a></li>
                    <% end %>
                </ul>
            </li>

            <li class="dropdown">
                <a data-toggle="dropdown">Stations<b class="caret"></b></a>
                <ul role="menu" class="dropdown-menu">
                    <li><%= link_to "Create New Station", new_station_path %></li>

                    <li class="divider"></li>
                    <% Station.all.order(:name).each do |station| %>
                        <li><a href="<%= station_path(station) %>"><%= station.name %></a></li>
                    <% end %>
                </ul>
            </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
            <li><a href="/">Home Page</a></li>
        </ul>

    </div>
</nav>
