<nav role="navigation" class="navbar navbar-default">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href="/" class="navbar-brand">Bus Tracker</a>
    </div>
    <!-- Collection of nav links, forms, and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            <li class="dropdown">
                <a data-toggle="dropdown" class="dropdown-toggle" href="<%= lines_path %>">Lines<b class="caret"></b></a>
                <ul role="menu" class="dropdown-menu">
                    <li><a href="<%= new_line_path %>">Create New Line</a></li>
                    <li class="divider"></li>
                    <% Line.all.order(:name).each do |line| %>
                        <li><a href=<%= line_path(line) %>><%= line.name %> </a></li>
                    <% end %>
                </ul>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a href="/">Home Page</a></li>
        </ul>
    </div>
</nav>
